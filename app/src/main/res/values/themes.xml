<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Ledgertrace" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/ledger_primary</item>
        <item name="colorSecondary">@color/ledger_primary</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowBackground">@color/ledger_background</item>
    </style>

    <style name="Theme.Ledgertrace" parent="Base.Theme.Ledgertrace" />
</resources>
